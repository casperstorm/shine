/* @flow */
import React from 'react'
import { storiesOf } from '@storybook/react-native'

import SettingsScreen from '../../../src/screens/settings'

storiesOf('settings', module).add('default', () => <SettingsScreen />)
